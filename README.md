About
customer.io has no way to notify you if a data integration fails (hubspot in my case) so i made a Gadget app to query a Customer.io segment that contains contacts made in the last day. If that segment is 0, this app emails the company slack channel to warn that the sync is likely broken.
